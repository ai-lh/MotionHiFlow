_target_: src.data.t2m_dataset.MotionDataset
mean: ${data.mean}
std: ${data.std}
opt:
  window_size: 64
  meta_dir: ${folder.run}/meta
  eval_mean_std: True
  is_train: True
  joints_num: ${data.meta.joints}
  feat_bias: 1.0
  motion_dir: ${data.data_root}/new_joint_vecs